<section>
    <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email-id</th>
      <th>Options</th> 
    </tr>

  </thead>
  <tbody>
    {{#each users}}
    <tr>
      <td>{{this.name}}</td>
      <td>{{this.email}}</td>
      <td><a href="/admin/edituser/{{this._id}}" class="btn btn-info">Edit</a>
      <a href="/admin/deleteuser/{{this._id}}" class="btn btn-danger" onclick="return confirm('Do you want to delete {{this.name}}')">Delete</a></td>
      </tr>
    {{/each}}
    
  </tfoot>
</table>
</section>

